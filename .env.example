# Gemini API key (server-side only — never exposed to the client).
# Get one at https://aistudio.google.com/apikey
# The app runs WITHOUT this key in degraded demo mode (canned responses + non-AI routing).
GEMINI_API_KEY=
